version: "3.8"
services:
  cache:
    image: redis:6.2-alpine
    env_file: "white.env"
    restart: always
    ports:
      - "6379:6379"
    command: redis-server --save 20 1 --loglevel warning --requirepass ${REDIS_PASSWORD}
    volumes:
      - cache:/data
volumes:
  cache:
    driver: local
